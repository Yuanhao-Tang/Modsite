% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motif_logo.R
\name{geom_logo}
\alias{geom_logo}
\title{ggplot2 layer for a sequence logo}
\usage{
geom_logo(
  data = NULL,
  method = "bits",
  seq_type = "auto",
  namespace = NULL,
  font = "roboto_medium",
  stack_width = 0.95,
  rev_stack_order = FALSE,
  col_scheme = "auto",
  low_col = "black",
  high_col = "yellow",
  na_col = "grey20",
  plot = TRUE,
  ...
)
}
\arguments{
\item{data}{Character vector of aligned sequences, a named list of
character vectors, or a custom matrix.}

\item{method}{Height method: `"bits"` (default), `"probability"`, or
`"custom"` (supply a numeric matrix with letter row names).}

\item{seq_type}{Sequence type: `"auto"` (default), `"aa"`, `"dna"`,
`"rna"`, or `"other"`.}

\item{namespace}{Custom alphabet for `seq_type = "other"`.}

\item{font}{Font name.  See [list_fonts()].  Default
`"roboto_medium"`.}

\item{stack_width}{Letter-stack width in (0, 1].  Default `0.95`.}

\item{rev_stack_order}{Reverse the stack order.  Default `FALSE`.}

\item{col_scheme}{Colour scheme name or a `modsite_cs` object from
[make_col_scheme()].  Default `"auto"`.}

\item{low_col, high_col}{Gradient endpoints for quantitative colour schemes.}

\item{na_col}{Colour for letters absent from the colour scheme.}

\item{plot}{If `FALSE`, return the plot data instead of a layer.}

\item{...}{Additional arguments forwarded to the polygon layer.}
}
\value{
A list of ggplot2 layer objects (or a `data.frame` when
  `plot = FALSE`).
}
\description{
Adds a sequence logo as a polygon layer to an existing (or new) ggplot.
Combine with [theme_logo()] for publication-quality output, or use the
convenience wrapper [ggseqlogo()] to create a complete plot.
}
\examples{
seqs <- c("ACGT", "ACGT", "ACTT", "ACGA")
p <- ggplot2::ggplot() + geom_logo(seqs) + theme_logo()
print(p)

}
