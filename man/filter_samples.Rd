% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merger.R
\name{filter_samples}
\alias{filter_samples}
\title{Remove samples with excessive missing-rate from a merged table}
\usage{
filter_samples(merger, max_missing_rate = 0.5, auto_merge = TRUE)
}
\arguments{
\item{merger}{A `MergeSamples` object.  [merge_samples()] must have been
called first (or set `auto_merge = TRUE`).}

\item{max_missing_rate}{Maximum allowed missing-rate per sample.
Must be in `[0, 1]`. Default `0.5`.}

\item{auto_merge}{If `TRUE` (default) and `merger$merged_data` is `NULL`,
[merge_samples()] is called automatically before filtering.}
}
\value{
The (invisibly modified) `merger` object with samples removed from
  all relevant fields.
}
\description{
Operates on `merger$merged_data`.  Samples whose fraction of `NA` sites
exceeds `max_missing_rate` are dropped from the data, `$sample_names`,
`$sample_files`, and `$condition`.
}
\examples{
\dontrun{
m <- new_merger(sample_files = files, condition = groups,
                modification_method = "PUMseq")
filter_samples(m, max_missing_rate = 0.4)
}

}
