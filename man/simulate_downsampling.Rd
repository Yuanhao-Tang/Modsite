% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/saturation.R
\name{simulate_downsampling}
\alias{simulate_downsampling}
\title{Simulate downsampling of sequencing depth}
\usage{
simulate_downsampling(analyzer, ratios = NULL, random_seed = NULL)
}
\arguments{
\item{analyzer}{A `SaturationAnalyzer` with `site_stats` already available.}

\item{ratios}{Numeric vector of downsampling ratios within (0, 1]. Defaults to
`seq(0.1, 1.0, by = 0.1)`.}

\item{random_seed}{Optional integer seed for reproducibility.}
}
\value{
A `data.frame` with columns `ratio` and `valid_sites`.
}
\description{
Given per-site total depth and modified read counts, simulate lower depth by
independently downsampling modified and unmodified reads with a binomial
model. A site is considered valid when:
`depth >= min_depth` and `rate >= min_mod_rate`.
}
\examples{
az <- new_saturation_analyzer("dummy.bam")
az$site_stats <- data.frame(total_depth = c(80L, 50L, 30L, 100L, 20L),
                            mod_count   = c(8L,  5L,  1L,  15L,  1L))
simulate_downsampling(az, ratios = c(0.5, 1.0), random_seed = 42L)

}
