% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merger_visualization.R
\name{plot_detection_consistency}
\alias{plot_detection_consistency}
\title{Assess detection consistency between samples}
\usage{
plot_detection_consistency(
  merger,
  sample_names = NULL,
  condition = NULL,
  show_heatmap = TRUE,
  show_score_bar = FALSE,
  plot_mode = c("separate", "combined")
)
}
\arguments{
\item{merger}{A `MergeSamples` object with non-`NULL` `$merged_data`, or a
merged site `data.frame`.}

\item{sample_names}{Optional sample-rate column names when `merger` is a
`data.frame`. If `NULL`, sample columns are auto-detected.}

\item{condition}{Optional grouping vector aligned to `sample_names`. If
omitted for a merger object, `merger$condition` is used.}

\item{show_heatmap}{Logical; if `TRUE` (default), print a Jaccard heatmap.}

\item{show_score_bar}{Logical; if `TRUE`, print a bar plot of average Jaccard
scores across samples.}

\item{plot_mode}{One of `"separate"` or `"combined"`. The current
implementation treats both the same and keeps the argument for backward
compatibility with legacy scripts.}
}
\value{
Invisibly returns a named list with components `matrix`, `scores`,
  `heatmap`, and `score_plot`.
}
\description{
Computes pairwise Jaccard similarity based on whether each site is detected
(`non-NA`) in each sample. The function can print a heatmap, a bar chart of
average per-sample consistency scores, or both. It invisibly returns the
underlying matrix, summary table, and any constructed plot objects.
}
