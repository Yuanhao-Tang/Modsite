% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metagene_core.R
\name{.map_sites_to_metagene}
\alias{.map_sites_to_metagene}
\title{Map modification sites onto normalised transcript coordinates}
\usage{
.map_sites_to_metagene(annotator, sites_df, split_strategy = "median")
}
\arguments{
\item{annotator}{A `GenomicAnnotator` object.}

\item{sites_df}{`data.frame` with at minimum columns `chrom`, `pos`, and
optionally `strand`.  Additional columns (sample values) are carried
through unchanged.}

\item{split_strategy}{Passed to `.calculate_region_splits()`.}
}
\value{
A named list with two elements:
  \describe{
    \item{`data`}{`data.table` with all original columns plus
      `tx_name`, `tx_pos`, `feature_pos`, and `feature_weight`.}
    \item{`splits`}{Named numeric vector of region proportions.}
  }
  Returns `NULL` if no sites could be mapped.
}
\description{
Map modification sites onto normalised transcript coordinates
}
\keyword{internal}
