% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diff_sites.R
\name{new_diff_sites}
\alias{new_diff_sites}
\title{Construct a two-group differential site analyzer}
\usage{
new_diff_sites(
  annotated_df,
  group1_samples,
  group2_samples,
  group1_name = "Group1",
  group2_name = "Group2",
  test_method = c("t-test", "wilcox-test"),
  min_abs_log2fc = 0,
  p_value_threshold = 0.05,
  adjust_method = "BH",
  min_samples_per_group = 2L,
  pseudocount = 1e-06
)
}
\arguments{
\item{annotated_df}{A merged/annotated `data.frame` with one row per site.
Must contain at least the columns named in `group1_samples` and
`group2_samples`.  Site metadata and annotation columns are preserved in
the output.}

\item{group1_samples}{Character vector of Group 1 sample column names
(e.g., treatment).}

\item{group2_samples}{Character vector of Group 2 sample column names
(e.g., control).}

\item{group1_name}{Display label for Group 1, used to name output columns
such as `<group1_name>_mean`.  Default `"Group1"`.}

\item{group2_name}{Display label for Group 2.  Default `"Group2"`.}

\item{test_method}{Statistical test: `"t-test"` (Welch two-sample t-test,
default) or `"wilcox-test"` (Wilcoxon rank-sum with continuity
correction).}

\item{min_abs_log2fc}{Minimum absolute log2 fold-change required to set
`pass_fc = TRUE`.  Default `0` (no fold-change gate).}

\item{p_value_threshold}{Adjusted p-value threshold for `significant`.
Default `0.05`.}

\item{adjust_method}{Multiple-testing correction method forwarded to
[stats::p.adjust()].  Default `"BH"` (Benjamini–Hochberg).}

\item{min_samples_per_group}{Minimum number of non-`NA` observations
required in each group per site.  Sites that do not meet this threshold
receive `NA` p-values.  Default `2`.}

\item{pseudocount}{Small constant added to both group means before computing
log2 fold-change to avoid log(0).  Default `1e-6`.}
}
\value{
A `DiffSites` list object.  Pass to [run_diff_sites()] to obtain
  results.
}
\description{
Creates a `DiffSites` object that encapsulates the input data, sample
groupings, and analysis parameters for a two-group comparison of per-site
RNA modification rates.  Call [run_diff_sites()] to execute the analysis.
}
\examples{
\dontrun{
ds <- new_diff_sites(
  annotated_df    = my_df,
  group1_samples  = c("trt1", "trt2", "trt3"),
  group2_samples  = c("ctrl1", "ctrl2", "ctrl3"),
  group1_name     = "Treatment",
  group2_name     = "Control"
)
results <- run_diff_sites(ds)
}

}
\seealso{
[run_diff_sites()], [save_diff_results()], [run_glmm()]
}
