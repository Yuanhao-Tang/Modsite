% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_mods.R
\name{annotate_known_mods}
\alias{annotate_known_mods}
\title{Annotate sites against a known-modification database}
\usage{
annotate_known_mods(
  annotator,
  sites_df,
  chrom_col = "chrom",
  pos_col = "pos",
  strand_col = "strand",
  match_strand = TRUE
)
}
\arguments{
\item{annotator}{A `ModAnnotator` object from [new_mod_annotator()].}

\item{sites_df}{A `data.frame` of sites to annotate.}

\item{chrom_col}{Name of the chromosome column. Default `"chrom"`.}

\item{pos_col}{Name of the position column (1-based). Default `"pos"`.}

\item{strand_col}{Name of the strand column. Default `"strand"`.}

\item{match_strand}{Logical.  If `TRUE` (default) and `strand_col` is
present in `sites_df`, strand must agree between site and database
entry.  Set to `FALSE` to ignore strand.}
}
\value{
`sites_df` with three additional columns appended:
  \describe{
    \item{`is_known_mod`}{Logical; `TRUE` when the site overlaps a
      database entry.}
    \item{`mod_id`}{Identifier of the matching database record
      (`NA` for unmatched sites).}
    \item{`mod_type`}{Modification type of the matching record
      (`NA` for unmatched sites).}
  }
}
\description{
For each site in `sites_df`, checks whether it overlaps any entry in
`annotator$mod_gr`.  When a match is found the site is flagged as a known
modification and annotated with the corresponding `ModID` and `ModType`.
If a site overlaps multiple database entries, the first match is used.
}
