% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_genome.R
\name{annotate_genomic_regions}
\alias{annotate_genomic_regions}
\title{Annotate site data frame with genomic region labels and gene metadata}
\usage{
annotate_genomic_regions(
  annotator,
  sites_df,
  chrom_col = "chrom",
  pos_col = "pos",
  strand_col = "strand"
)
}
\arguments{
\item{annotator}{A `GenomicAnnotator` object from [new_genomic_annotator()].}

\item{sites_df}{A `data.frame` of sites to annotate.  Must contain at
least `chrom_col` and `pos_col`; `strand_col` is used when present.}

\item{chrom_col}{Name of the chromosome column. Default `"chrom"`.}

\item{pos_col}{Name of the position column (1-based). Default `"pos"`.}

\item{strand_col}{Name of the strand column. Default `"strand"`.}
}
\value{
`sites_df` with five additional columns appended:
  `gene_id`, `gene_name`, `gene_type`, `tx_name`, `genomic_region`.
}
\description{
Each site is assigned to the highest-priority region that overlaps it,
following the order: 5'UTR > CDS > 3'UTR > exon > intron > intergenic.
The best-matching transcript is selected by transcript support level
(lowest numeric TSL wins); ties are broken by transcript length
(longest wins).
}
