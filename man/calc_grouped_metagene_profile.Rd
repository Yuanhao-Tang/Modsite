% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metagene_grouped.R
\name{calc_grouped_metagene_profile}
\alias{calc_grouped_metagene_profile}
\title{Compute grouped metagene profiles}
\usage{
calc_grouped_metagene_profile(
  analyzer,
  group_by,
  group_cutoff = 0.05,
  group_breaks = NULL,
  group_labels = NULL,
  stat = c("density", "count"),
  weight_by = NULL,
  effect_col = NULL,
  p_col = NULL,
  padj_col = NULL,
  filter = NULL,
  smooth = TRUE,
  span = 0.3,
  include_na_group = FALSE
)
}
\arguments{
\item{analyzer}{A `MetageneAnalyzer` object returned by
[new_metagene_analyzer()].}

\item{group_by}{Column name in the mapped site table used for grouping.}

\item{group_cutoff}{Numeric cutoff used when `group_by` is numeric and
`group_breaks` is `NULL`. Default `0.05`.}

\item{group_breaks}{Optional numeric vector of cut points for numeric
grouping. When supplied, overrides `group_cutoff`.}

\item{group_labels}{Optional labels for numeric groups. Must have length
`length(group_breaks) - 1` when `group_breaks` is supplied.}

\item{stat}{Summary statistic for each group: `"density"` (default) scales
each group to sum to 1 after smoothing; `"count"` returns weighted counts.}

\item{weight_by}{Optional weighting rule. Use `NULL` for unweighted site
counts, a numeric column name from the mapped table, or one of
`"abs_effect"`, `"minus_log10_p"`, or `"minus_log10_padj"`.}

\item{effect_col}{Numeric column used when `weight_by = "abs_effect"`.}

\item{p_col}{Numeric column used when `weight_by = "minus_log10_p"`.}

\item{padj_col}{Numeric column used when `weight_by = "minus_log10_padj"`.}

\item{filter}{Optional one-sided formula used to keep rows before profile
computation, for example `~ fit_ok & !or_extreme`.}

\item{smooth}{Logical. Apply spline smoothing. Default `TRUE`.}

\item{span}{Smoothing parameter passed to `smooth.spline(spar=)`. Default
`0.3`.}

\item{include_na_group}{Logical. Keep `NA` grouping values as a separate
`"NA"` group. Default `FALSE`.}
}
\value{
The input `analyzer` with `grouped_profile_data` and
  `grouped_profile_spec` filled in.
}
\description{
Creates one metagene curve per group from a mapped `MetageneAnalyzer`
object. Groups can be defined by categorical columns directly, or by
splitting numeric columns using a cutoff or custom breaks.
}
\examples{
\dontrun{
mg <- new_metagene_analyzer(ann, sites_df = glmm_res$primary_term_backfill)
mg <- calc_grouped_metagene_profile(
  mg,
  group_by = "motif",
  stat = "density",
  filter = ~ fit_ok & !or_extreme
)
}

}
