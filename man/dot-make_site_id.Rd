% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.make_site_id}
\alias{.make_site_id}
\title{Build a character site identifier from a data frame}
\usage{
.make_site_id(df)
}
\arguments{
\item{df}{A `data.frame` with at least columns `chrom`, `pos`, and `ref`.
An optional `strand` column is used when present.}
}
\value{
A character vector of the same length as `nrow(df)`.
}
\description{
Produces `chrom_pos_ref` or `chrom_pos_ref_strand` depending on whether a
`strand` column is present.  The result is used as a join key throughout the
package and must be consistent across all callers.
}
\keyword{internal}
