% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metagene_io.R
\name{load_metagene_sites}
\alias{load_metagene_sites}
\title{Load site data for metagene analysis}
\usage{
load_metagene_sites(file_path, format = "auto", has_header = TRUE)
}
\arguments{
\item{file_path}{Path to the input file.}

\item{format}{File format: `"auto"` (default, inferred from extension),
`"tsv"`, `"csv"`, or `"bed"`.}

\item{has_header}{Logical.  Whether the file has a header row (ignored for
BED format, which never has a header).  Default `TRUE`.}
}
\value{
A `data.table` with at least columns `chrom` and `pos`.  BED files
  additionally produce a `strand` column when six or more columns are
  present.
}
\description{
Reads a delimited or BED file and returns a `data.table` suitable for
passing to [new_metagene_analyzer()].  The result is guaranteed to contain
at least the columns `chrom` and `pos`.
}
\details{
Column name aliases applied automatically:
\itemize{
  \item `Chromosome` → `chrom`
  \item `Start` → `pos` (only when no `pos` column already exists)
}
}
\examples{
\dontrun{
sites <- load_metagene_sites("sites.tsv")
sites <- load_metagene_sites("peaks.bed", format = "bed")
}

}
