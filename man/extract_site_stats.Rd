% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/saturation.R
\name{extract_site_stats}
\alias{extract_site_stats}
\title{Extract per-site depth and modification counts from BAM pileup}
\usage{
extract_site_stats(analyzer)
}
\arguments{
\item{analyzer}{A `SaturationAnalyzer` object created by
[new_saturation_analyzer()].}
}
\value{
A `data.frame` with columns `total_depth` and `mod_count`.
}
\description{
This function uses `Rsamtools::pileup()` to obtain nucleotide counts.
For each genomic position, the most frequent nucleotide is treated as the
reference, and the remaining counts are treated as "modified" reads.
}
\examples{
\dontrun{
az    <- new_saturation_analyzer("sample.bam")
stats <- extract_site_stats(az)
head(stats)
}

}
