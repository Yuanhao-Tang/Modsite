% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merger_visualization.R
\name{plot_sample_correlation}
\alias{plot_sample_correlation}
\title{Plot sample-by-sample Pearson correlations}
\usage{
plot_sample_correlation(
  merger,
  sample_names = NULL,
  condition = NULL,
  show_numbers = FALSE
)
}
\arguments{
\item{merger}{A `MergeSamples` object with non-`NULL` `$merged_data`, or a
merged site `data.frame`.}

\item{sample_names}{Optional sample-rate column names when `merger` is a
`data.frame`. If `NULL`, sample columns are auto-detected.}

\item{condition}{Optional grouping vector aligned to `sample_names`. When
supplied (or present in a merger object), condition values are appended to
sample labels on both heatmap axes.}

\item{show_numbers}{Logical; if `TRUE`, overlay correlation values on tiles.}
}
\value{
A `ggplot` object.
}
\description{
Computes a pairwise Pearson correlation matrix across samples using
`pairwise.complete.obs` and visualises it as a heatmap.
}
