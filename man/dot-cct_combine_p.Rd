% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate_cct.R
\name{.cct_combine_p}
\alias{.cct_combine_p}
\title{Combine p-values using the Cauchy Combination Test}
\usage{
.cct_combine_p(p, weights = NULL)
}
\arguments{
\item{p}{Numeric vector of site-level p-values in \eqn{[0, 1]}.}

\item{weights}{Non-negative numeric weight vector (same length as `p`).
`NULL` means equal weights.}
}
\value{
A single numeric p-value in \eqn{[0, 1]}, or `NA_real_` if `p` is
  empty.
}
\description{
Computes the CCT statistic
\deqn{T = \sum_i w_i \tan\bigl((0.5 - p_i)\pi\bigr)}
and returns the one-sided p-value \eqn{P(C > T)} where \eqn{C} follows a
standard Cauchy distribution.  Numerically stable for \eqn{p} near 0 or 1.
}
\keyword{internal}
